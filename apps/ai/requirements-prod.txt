# Runtime-only dependencies for the FastAPI inference container.
# Training, linting, type checking, and test tools stay in requirements.txt.
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=18.0.0
pyyaml>=6.0.0
lightgbm>=4.5.0
scipy>=1.14.0

# Điều phối multi-agent — LangGraph được duyệt lại 2026-08-23 (CLAUDE.md §6 #3).
# httpx là dependency runtime cho LLM gateway (không dùng SDK nhà cung cấp).
langgraph>=1.2,<2
httpx>=0.28.0
