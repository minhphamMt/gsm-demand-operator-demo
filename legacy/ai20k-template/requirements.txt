# -*- coding: utf-8 -*-
# Dòng trên là bắt buộc: pip đọc file này bằng encoding của hệ điều hành, trên
# Windows là cp1252 và sẽ vỡ ở ký tự tiếng Việt nếu thiếu khai báo.
# GSM-14 NovaFour — bộ dependency đã duyệt (CLAUDE.md §6).
# Thêm gói mới phải hỏi trước. Danh sách gói bị cấm: CLAUDE.md §6 #3–#4.
# (Không nhắc tên gói bị cấm ở đây — test tĩnh của T0.5 grep chính chuỗi đó.)

# ---- Web ----
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# ---- Dữ liệu & mô hình ----
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=18.0.0
pyyaml>=6.0.0
lightgbm>=4.5.0
scikit-learn>=1.5.0
scipy>=1.14.0

# ---- Dev ----
ruff>=0.8.0
mypy>=1.13.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
